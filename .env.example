# Convex
# For local development, run `pnpm exec convex dev` which creates .env.local
# For production, set these in your hosting provider

# Local development (auto-generated by `convex dev`)
# CONVEX_DEPLOYMENT=dev:your-project-name
# VITE_CONVEX_URL=https://your-project.convex.cloud

# Production
# VITE_CONVEX_URL=https://your-production-project.convex.cloud

# Optional: OpenAI for LLM features (future)
# OPENAI_API_KEY=sk-...

# Claude Code CLI path (for Claude Agent SDK)
# Required for Claude provider - path to the Claude Code CLI executable
# Find with: which claude
# CLAUDE_CODE_PATH=/home/user/.local/bin/claude
